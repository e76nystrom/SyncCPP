T_PARM remparm[] =
{
 PARM(syncCycle),               /* 0x00 sync cycle length */
 PARM(syncOutput),              /* 0x01 sync outputs per cycle */
 PARM(syncPrescaler),           /* 0x02 sync prescaler */
 PARM(syncEncoder),             /* 0x03 sync encoder pulses */
 PARM(syncMaxParm),             /* 0x04 sync maximum parameter */
};
