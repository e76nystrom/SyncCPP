 EXT uint16_t syncCycle;        /* 0x00 sync cycle length */
 EXT uint16_t syncOutput;       /* 0x01 sync outputs per cycle */
 EXT uint16_t syncPrescaler;    /* 0x02 sync prescaler */
 EXT uint16_t syncEncoder;      /* 0x03 sync encoder pulses */
 EXT int16_t syncMaxParm;       /* 0x04 sync maximum parameter */
